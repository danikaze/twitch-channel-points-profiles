import { Resource } from 'i18next';
import en from './en';
import es from './es';
import ja from './ja';

// static locales loaded at the start is enough for now
export const resources: Resource = {
  en,
  es,
  ja,
};
